[build]
builder = "nixpacks"

[phases.install]
cmds = ["python -m pip install -r requirements.dev.txt"]

git log --oneline -3

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
